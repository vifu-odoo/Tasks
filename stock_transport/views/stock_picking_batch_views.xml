<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View -->
    <record id="stock_picking_batch_form" model="ir.ui.view">
        <field name="name">stock.picking.batch.form.inherit.stock.transport</field>
        <field name="model">stock.picking.batch</field>
         <field name="inherit_id" ref="stock_picking_batch.stock_picking_batch_form"/>
        <field name="arch" type="xml">
            <field name="scheduled_date" position="replace">
                <group class="w-100">
                    <field name="dock_id"/>
                    <field name="vehicle" placeholder="Third Party Provider"/>
                    <field name="vehicle_category" placeholder="e.g. Semi-truck"/>
                </group>
                <group class="w-100" invisible="not vehicle_category">
                    <label for="weight_progress"/>
                    <div class="o_row w-100">
                        <field name="total_weight"/>
                        <field name="weight_progress" widget="progressbar" class="oe_inline"/>
                    </div>

                    <label for="volume_progress"/>
                    <div class="o_row w-100">
                        <field name="total_volume"/>
                        <field name="volume_progress" widget="progressbar" class="oe_inline"/>
                    </div>
                </group>
            </field> 
        </field>
    </record> 

    <!-- Graph view -->
    <record id="view_stock_picking_batch_graph" model="ir.ui.view">
        <field name="name">stock.picking.batch.graph</field>
        <field name="model">stock.picking.batch</field>
        <field name="arch" type="xml">
            <graph string="Batch Transfers graph">
                <field name="scheduled_date" type='row'/>
                <field name="vehicle_category" type='column'/>
                <field name="lines"/>
                <field name="transfers"/>
                <field name ="volume_progress"/>
                <field name ="weight_progress"/>
            </graph>
        </field>
    </record>

    <!-- Gantt View -->
    <record id="view_stock_picking_batch_gantt" model="ir.ui.view">
        <field name="name">stock.picking.batch.gantt</field>
        <field name="model">stock.picking.batch</field>
        <field name="arch" type="xml">
            <gantt date_start="scheduled_date" date_stop="scheduled_date" string="Transport Operations" default_group_by='dock_id,vehicle' default_scale="week" thumbnails="{'vehicle': 'image_128'}" color="vehicle" decoration-info="state == 'draft'" decoration-success="state == 'done'">
                <field name="state"/>
            </gantt>
        </field>
    </record>

    <record id="stock_picking_batch.stock_picking_batch_action" model="ir.actions.act_window">
        <field name="view_mode">tree,form,kanban,graph,gantt</field>
    </record>
</odoo>